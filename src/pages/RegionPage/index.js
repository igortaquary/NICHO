import React from 'react';
import PhotosGrid from '../../components/PhotosGrid';

const RegionPage = ({navigation}) => {
    return(
        <PhotosGrid navigation={navigation}/>
    )
};

export default RegionPage;